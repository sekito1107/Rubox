<div class="flex flex-col h-full bg-white dark:bg-[#0D1117] transition-colors" data-controller="editor--runner">
  <%= render Layout::HeaderComponent.new %>

  <div class="flex-1 flex min-h-0">
    <div class="flex-1 flex flex-col min-w-0">
      <%= render Editor::MainComponent.new %>
      <%= render Terminal::OutputComponent.new %>
    </div>

    <%= render Rurema::SidebarComponent.new %>
  </div>
</div>
